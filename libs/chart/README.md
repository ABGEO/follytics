# @follytics/charts

A lightweight, customizable SVG chart generation library for Follytics applications. This package provides server-side SVG chart generation using D3.js.

## Building

Run `nx build chart` to build the library.

## Running unit tests

Run `nx test chart` to execute the unit tests via [Jest](https://jestjs.io).
